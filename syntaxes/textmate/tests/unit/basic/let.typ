#let x = 1;
#let (x) = 1;
#let (x, y) = (1, 1);
#let (x, (y, z)) = (1, (1, 1));
#let (a: a) = (a: 1);
#let (a, (b: b)) = (1, (b: 1));
#let (a, (b: (c, (b: b)))) = (a, (b: (1, (b: b))));
#let ((b: (c, (b: b))), a) = ((b: (1, (b: b))), a);
#let (a, ..) = (1, 1);
#let (.., a) = (1, 1);
#let (a: a, ..) = (a: a);
#let (.., a: a) = (a: a);
#let (a: a, .., b: b) = (a: a, b: b);